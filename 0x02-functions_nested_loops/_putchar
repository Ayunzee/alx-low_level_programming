#include <unistd.h>
/**
 * _putchar - this whrites the character c to standard output
 * @c: the charceter to be printed
 *
 * Return:(1) 0n success.
 * on error, -1 is returned, and erreo is set approximately.
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}
